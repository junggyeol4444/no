# WebNovel Studio 사용법 (아주 쉬움)

터미널·명령어 없이 쓰는 방법입니다.

---

## 1. 딱 한 번만: Node.js 설치

이 프로그램을 돌리려면 컴퓨터에 **Node.js** 가 한 번은 깔려 있어야 합니다.
(안 깔려 있으면 아래 실행 파일이 자동으로 설치 페이지를 열어줍니다.)

- https://nodejs.org/ko 접속 → **LTS** 버튼 클릭해서 설치 → “다음/동의”만 계속 누르면 끝.

## 2. 실행 (더블클릭)

내 운영체제에 맞는 파일을 **더블클릭**하세요.

| 운영체제 | 실행할 파일 |
| --- | --- |
| **윈도우** | `실행-윈도우.bat` |
| **맥** | `실행-맥.command` |
| **리눅스** | `실행-리눅스.sh` |

- 처음 한 번은 “설치 중…”이 몇 분 걸립니다. (그 다음부터는 빠릅니다.)
- 잠시 뒤 **브라우저가 자동으로 열립니다** (`http://localhost:3000`).
  - 안 열리면 브라우저 주소창에 `localhost:3000` 을 직접 치세요.
- **끄기**: 검은 창(터미널)을 닫으면 종료됩니다.

> 맥에서 “확인되지 않은 개발자” 경고가 뜨면: `실행-맥.command` 파일을 **마우스 우클릭 → 열기** 로 한 번만 실행하면 이후엔 더블클릭됩니다.

## 3. AI로 글쓰기 준비 (둘 중 하나)

이 도구는 글을 **AI 모델**에게 시킵니다. 둘 중 편한 걸 고르세요.

### (가) 무료·오프라인 — 로컬 모델 (기본값)
1. https://ollama.com 에서 Ollama 설치.
2. 설치 후 한 번, 명령 프롬프트/터미널에 아래를 입력해 모델을 내려받습니다:
   ```
   ollama pull qwen2.5:7b
   ```
   (컴퓨터가 가벼우면 `ollama pull qwen2.5:3b`)
3. 끝. 앱에서 바로 “✨ 자동 생성”이 됩니다.

### (나) 고품질 — 클라우드 (Claude, 유료 키)
1. 앱 오른쪽 위 **⚙️ AI 설정** → “클라우드(Claude)” 선택.
2. 프로젝트 폴더의 `.env.local` 파일에 API 키를 넣습니다. (`.env.example` 참고)

> 작은 로컬 모델은 한국어 장문이 약합니다. **문장 품질**이 중요하면 (나) Claude 나 큰 로컬 모델(14B+)을 권장합니다.

## 4. 이렇게 쓰면 됩니다

1. **새 작품** 만들기 → 제목·장르 입력.
2. **캐릭터/세계관** 등록 (직접 입력하거나, 가진 설정 글을 붙여넣고 “자동 분석”).
3. **회차 목록 → 새 회차 → ✨ 자동 생성** — 실시간으로 본문이 써집니다.
4. 마음에 들면 **발행** → 우리 **독자 사이트**(오른쪽 위 “📖 독자 사이트”)에 올라갑니다.
5. **내보내기**: 회차 목록에서 txt/md 로 저장해 다른 곳에 붙여넣기.

---

문제가 생기면 검은 창(터미널)에 뜬 메시지를 그대로 캡처해서 알려주시면 됩니다.
